import { TruncateDecimals2Pipe } from './truncate-decimals2.pipe';

describe('TruncateDecimals2Pipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateDecimals2Pipe();
    expect(pipe).toBeTruthy();
  });
});
